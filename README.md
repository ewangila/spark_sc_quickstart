# PySpark Context Inspector

A simple script to initialize a SparkContext and inspect core runtime properties, resource allocation, and system configurations.

## Requirements
* Python
* Apache Spark / PySpark

## Usage
```bash
python <script_name>.py
